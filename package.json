{
  "dependencies": {
    "@google/generative-ai": "^0.22.0",
    "dotenv": "^16.4.7"
  },
  "scripts": {
    "build": "node build.js && mkdir -p dist && cp *.html *.js *.css dist/"
  },
  "devDependencies": {
    "dotenv-webpack": "^8.1.0"
  }
}
